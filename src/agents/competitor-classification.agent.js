export async function competitorClassificationAgent(rawCompetitors, structuredIdea) {
  // TODO: Implement real classification (direct / indirect / alternatives).
  return {
    direct: [],
    indirect: [],
    alternatives: [],
    raw: rawCompetitors,
  };
}
