import { pool } from '../config/db.js';

export async function saveAnalysisResult(result) {
  // TODO: Implement real persistence.
  // This is a stub to keep the data layer explicit.
  return result;
}

